# Flask JWT secret key (required for token signing)
JWT_SECRET_KEY=your-very-secret-key

# SQLAlchemy database URI (default is SQLite file in instance folder)
SQLALCHEMY_DATABASE_URI=sqlite:///app.db

# Add any other environment variables your app needs below
# Example:
# FLASK_ENV=development
# SECRET_KEY=another-secret-if-needed